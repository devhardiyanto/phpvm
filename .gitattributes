# Shell scripts and bats tests must stay LF — CRLF breaks them on Linux/macOS
# (`bad interpreter: /usr/bin/env bash^M`).
*.sh   text eol=lf
*.bats text eol=lf

# PowerShell is fine with CRLF on Windows; keep it consistent there.
*.ps1  text eol=crlf
*.psd1 text eol=crlf
*.cmd  text eol=crlf
